extends base

block content
    #recipes.container
        h1.title= title

extends base

block content
    #recipes.container
        h1.title= title
        ul
            each link, i in links.rows
